services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}" ]
      interval: 5s
      timeout: 3s
      retries: 5

  migrator:
    build: .
    image: tugrikbot
    entrypoint: [ "/app/bin/migrate" ]
    command: [ "up" ]
    environment:
      TUGRIK_DB_DSN: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}?sslmode=disable
    depends_on:
      db:
        condition: service_healthy

  bot:
    build: .
    image: tugrikbot
    entrypoint: [ "/app/bin/bot" ]
    environment:
      TUGRIK_BOT_TOKEN: ${TUGRIK_BOT_TOKEN}
      TUGRIK_DB_DSN: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_USER}?sslmode=disable
    depends_on:
      migrator:
        condition: service_completed_successfully
      db:
        condition: service_healthy
volumes:
  pgdata:
